---

✅ STAGE-BY-STAGE CHECKLIST FOR NETWORK CI/CD CHANGE (300 DEVICES)

Enterprise-grade, operationally safe, audit-ready.


---

0. CHANGE MANAGEMENT (Before Any CI/CD Execution)

0.1 Raise Change Request

[ ] Create formal change (e.g., CHG-1234)

[ ] Add scope: 300 routers

[ ] Add risk analysis

[ ] Add planned procedure

[ ] Add rollback plan

[ ] Add testing plan

[ ] Add success criteria

[ ] Add backout window and change window


0.2 Change Approval

[ ] CAB/Manager approves

[ ] Implementation time window confirmed



---

1. DEVELOPMENT STAGE (Pre-CI/CD)

1.1 Multi-Developer Workflow

[ ] Developers create branch: feat/chg-1234-router-desc

[ ] Update Jinja2 templates (interface + description)

[ ] Update router inventory

[ ] Update group_vars/host_vars if needed

[ ] Update Ansible playbooks

[ ] Update pyATS validation tests

[ ] Update MySQL logging utilities (if needed)


1.2 Code Review (MR/PR)

[ ] Submit Merge Request/Pull Request

[ ] Peer review completed

[ ] Static linting (optional pre-check pipeline)

[ ] Approvals collected

[ ] Merge allowed only after required approvals


1.3 Merge Final Code

[ ] Code merged to main or release branch

[ ] Repo triggers webhook → Jenkins



---

2. JENKINS PIPELINE STAGE CHECKLIST


---

Stage 0 — Device Reachability Gate (300 Routers)

2.0.1 Ping Check

[ ] Ping each router with:

2 packets per attempt

success = 0% packet loss


[ ] Retry up to 3 times

[ ] 10-second interval between retries


2.0.2 Reachability Result Evaluation

[ ] Collect:

reachable_count

unreachable list


[ ] If 300/300 reachable → proceed


2.0.3 Partial Reachability Handling

If fewer than 300 reachable (example 280/300):

[ ] Jenkins waits for operator input (max 2 min):

Continue with reachable devices?

Abort change?


[ ] If operator selects NO:

mark change failed (pre-deploy)

log to MySQL

send notification

exit pipeline


[ ] If operator selects YES OR timeout:

use only reachable subset

update inventory file

write per-device reachability to MySQL




---

Stage 1 — Prepare Workspace

[ ] Cleanup workspace

[ ] SCM checkout

[ ] Load Jenkins credentials

[ ] Load MySQL credentials

[ ] Load Ansible vault/password



---

Stage 2 — Build

[ ] Validate presence of:

requirements.txt

requirements.yml


[ ] Install Python dependencies

[ ] Install Ansible roles

[ ] Create vault temp file

[ ] Validate Jinja2 template syntax



---

Stage 3 — Test (Lint & Validation)

[ ] yamllint

[ ] ansible-lint

[ ] pyATS testbed validation

[ ] If failure:

log in MySQL

send notifications

fail pipeline




---

Stage 4 — Pre-Change Snapshot

[ ] For all selected devices (300 or 280):

run “show running-config”

run “show interface <iface>”


[ ] Save into pre_snapshots/<device>.txt

[ ] Write snapshot metadata to MySQL



---

Stage 5 — CANARY DRY RUN (5 Routers)

5.1 Dry Run

[ ] Select 5 canary routers

[ ] Render Jinja2 template → config preview

[ ] Run Ansible in --check mode

[ ] Evaluate dry-run output


5.2 Dry Run Decision

[ ] If canary dry-run fails:

log to MySQL

send notification

fail pipeline




---

Stage 6 — CANARY REAL APPLY (5 Routers)

6.1 Apply Changes

[ ] Apply configuration on 5 canary routers

[ ] Collect post-change interface outputs

[ ] Validate via pyATS:

description applied

interface up (if required)



6.2 Canary Apply Decision

[ ] If ANY canary fails:

rollback on failed devices

log to MySQL

notify failure

stop pipeline




---

Stage 7 — MAIN ROLLOUT (Batches of 30 Routers)

7.1 Batch Formation

[ ] Total devices (minus 5 canaries)

[ ] Divide into batches of 30

[ ] Final batch may be <30


7.2 For Each Batch

[ ] Render Jinja2 template for each device

[ ] Apply Ansible playbook

[ ] Capture per-device results

[ ] Write each device’s result to MySQL

[ ] Optional: post-check on sample devices


7.3 Batch Failure Decision

If a batch contains failures:

[ ] Prompt operator:

Continue skip failed?

Rollback failed?

Abort entire change?



If user chooses:

Continue:

skip failed devices

record failures in MySQL


Rollback failed:

rollback only failed devices

decide:

continue next batch OR stop change



Timeout (default continue):

skip failed devices

log policy-triggered continue in MySQL




---

Stage 8 — Post-Change Snapshot & Validation

8.1 Snapshot

[ ] Collect post-change config per device

[ ] Compare pre vs post (diff)

[ ] Store diffs in post_snapshots


8.2 Validation

[ ] pyATS interface description match

[ ] interface operational correctness

[ ] write validation status per device to MySQL



---

Stage 9 — Declarative Post Actions

[ ] Clean temp vault files

[ ] Clean rendered config files

[ ] Archive artifacts (logs, diffs)

[ ] Update MySQL:

pipeline_runs summary

device_change_logs entries


[ ] Update inventory:

list reachable devices

list skipped/failed devices


[ ] Notifications:

Email + Slack/Webex

Indicate CHG-ID, run ID, success/failure


[ ] If failure % > threshold:

auto-mark change as failed

auto-rollback if policy configured




---

Stage 10 — Grafana Publishing

Dashboards Generated

[ ] Time-series view (x-axis = date/time)

[ ] Change summary table (CHG-ID, result, devices touched)

[ ] Per-device result table (300 rows)

[ ] Offline devices view (from reachability stage)

[ ] Batch performance (batch vs success %)

[ ] Failure heatmap (optional)



---

⭐ OPTIONAL ADD-ONS (Industry-grade Enhancements)

A. Canary expansion

If 5 succeed, run 10-device sample before full batch.

B. Automatic region-based grouping

Rollout per location (POP1 → POP2 → POP3).

C. Automated ticket creation/close

Integrate Jira/ServiceNow via API.

D. Predictive “blast radius check”

Pre-check routing adjacencies before change.

E. Instant rollback-on-impact

If pyATS detects protocol flap → rollback immediately.


---




Advanced CI/CD + Change Control Flow

Formal change request + approval

Multi-dev coding & MR approvals

Git push → GitHub/GitLab

Jenkins pipeline with:

Reachability gate (300 pings, retries)

Smart handling of partial reachability

5-device canary dry-run + real apply

Batch rollout in 30s with decision points

Rollback logic

Inventory + MySQL updates

Grafana publishing



---

🔧 Advanced CI/CD + Change Control Flow

(1) FORMAL CHANGE MANAGEMENT
─────────────────────────────────────────────────────────────────────────────
   ┌─────────────────────────┐
   │  Change Raised          │
   │  - Change ID (CHG-1234) │
   │  - Scope: 300 routers   │
   │  - Window, risk, plan   │
   └─────────────┬───────────┘
                 │
                 ▼
   ┌─────────────────────────┐
   │ Change Approval Board   │
   │ (CAB / Manager / Lead)  │
   └─────────────┬───────────┘
         Approved│
                 ▼
(2) DEV & CODE REVIEW WORKFLOW
─────────────────────────────────────────────────────────────────────────────
   ┌─────────────────────────────────────────────────────────────┐
   │  Multiple Developers                                       │
   │  - DevA, DevB, DevC...                                     │
   │  - Work on feature branch: feat/chg-1234-int-desc          │
   │  - Update:                                                 │
   │      * Jinja2 template (interface desc)                    │
   │      * Inventory / vars for 300 routers                    │
   │      * Ansible playbooks + tests                           │
   └─────────────┬──────────────────────────────────────────────┘
                 │
                 │  git commit (local)
                 │
                 ▼
   ┌─────────────────────────────────────────────────────────────┐
   │  Create Merge Request / Pull Request                        │
   │  - Code review by peers                                     │
   │  - Static checks (optional pre-MR CI)                       │
   │  - Approvals required (Lead / Architect)                    │
   └─────────────┬──────────────────────────────────────────────┘
                 │ MR approved
                 ▼
   ┌─────────────────────────────────────────────────────────────┐
   │   Merge to main / release branch                            │
   │   Repo: GitHub or GitLab                                    │
   └─────────────┬──────────────────────────────────────────────┘
                 │
                 │ git push (merge)
                 ▼
(3) GIT REPO & WEBHOOK
─────────────────────────────────────────────────────────────────────────────
   ┌─────────────────────────────────────────────────────────────┐
   │   GitHub / GitLab                                           │
   │   - Stores main branch                                      │
   │   - Emits webhook on push to main                           │
   └─────────────┬──────────────────────────────────────────────┘
                 │ HTTP POST /webhook
                 ▼
(4) JENKINS CI/CD PIPELINE (HIGH-LEVEL)
─────────────────────────────────────────────────────────────────────────────
   ┌─────────────────────────────────────────────────────────────┐
   │                        Jenkins                              │
   │        (Multibranch Pipeline with Jenkinsfile)              │
   └─────────────┬──────────────────────────────────────────────┘
                 │
                 ▼
   ┌─────────────────────────────────────────────────────────────┐
   │ STAGE 0: DEVICE REACHABILITY CHECK                          │
   │  - For all 300 routers:                                     │
   │      ping -c 2 -W timeout                                   │
   │  - If ping fails: retry up to 3 times with 10s interval      │
   │                                                             │
   │  RESULT:                                                    │
   │    * reachable_count (e.g. 300, or 280)                     │
   │    * unreachable list                                       │
   └─────────────┬──────────────────────────────────────────────┘
                 │
   ┌─────────────┴─────────────────────────────────────┐
   │ DECISION A: ALL REACHABLE?                        │
   │  IF 300 / 300 reachable → continue automatically  │
   │  ELSE (e.g. 280/300 reachable)                    │
   └─────────────┬─────────────────────────────────────┘
                 │
                 ├─────────────────────────────────────────────────────┐
                 │                                                     │
                 ▼                                                     ▼
        PATH A: 300/300 OK                                   PATH B: SOME OFFLINE
        ─────────────────────────────────────          ─────────────────────────────────
        ┌────────────────────────────────────────┐     ┌───────────────────────────────┐
        │ Continue to Pre-Change Snapshot &      │     │  PROMPT USER (2 min timeout) │
        │ Canary Testing                         │     │                               │
        └────────────────────────────────────────┘     │  "Only 280/300 are reachable.│
                                                       │   Continue with 280 devices?  │
                                                       └─────────────┬─────────────────┘
                                                                     │
                                        ┌────────────────────────────┴─────────────────────────────┐
                                        │ DECISION B: USER INPUT                                  │
                                        └────────────────────────────┬─────────────────────────────┘
                                                                     │
                          ┌──────────────────────────────────────────┼───────────────────────────────────┐
                          │                                          │                                   │
                          ▼                                          ▼                                   ▼
                 OPTION B1: USER SAYS "NO"                  OPTION B2: USER SAYS "YES"          OPTION B3: NO INPUT
                 ────────────────────────────               ────────────────────────────         ─────────────────────
       ┌────────────────────────────────────────┐   ┌────────────────────────────────────┐   ┌───────────────────────┐
       │ - Mark change as FAILED (pre-deploy)   │   │ - Proceed with 280 reachable devs │   │ - After 2 min timeout │
       │ - Log to MySQL: change failed due to   │   │ - Inventory auto-updated to 280   │   │   default → CONTINUE  │
       │   offline devices                      │   │ - MySQL updated: reachability     │   │   with 280 reachable  │
       │ - Notify via email/Slack/tool          │   │   info per device                 │   │ - Same as B2 path     │
       │ - Pipeline ends                        │   └────────────────────────────────────┘   └───────────────────────┘


(5) PRE-CHANGE SNAPSHOTS & CANARY TESTING
─────────────────────────────────────────────────────────────────────────────
   (Executed for either 300 devices (Path A) or reachable subset (Path B2/B3))

   ┌─────────────────────────────────────────────────────────────┐
   │ STAGE 1: PRE-CHANGE SNAPSHOT                               │
   │  - Collect baseline from all selected devices:             │
   │      * show running-config                                  │
   │      * show interface <target>                              │
   │  - Store in: pre_snapshots/<device>.txt                     │
   │  - Log snapshot start/end to MySQL                         │
   └─────────────┬──────────────────────────────────────────────┘
                 │
                 ▼
   ┌─────────────────────────────────────────────────────────────┐
   │ STAGE 2: CANARY DRY RUN (5 DEVICES)                         │
   │  - Select 5 canary routers (critical / representative)      │
   │  - Render JINJA2 template for those routers                 │
   │  - Run Ansible in --check (dry-run)                         │
   │  - Compare proposed config vs baseline for validation       │
   └─────────────┬──────────────────────────────────────────────┘
                 │
   ┌─────────────┴─────────────────────────────────────┐
   │ DECISION C: CANARY DRY RUN OK?                    │
   └─────────────┬─────────────────────────────────────┘
                 │
    ┌────────────┼─────────────────────────────┐
    │            │                             │
    ▼            ▼                             ▼ (optional prompt)
FAIL PATH 1  SUCCESS PATH 1                     (you could ask for manual approval here)

FAIL: DRY RUN FAILED                           SUCCESS: PROCEED
────────────────────────                       ──────────────────────────────
┌────────────────────────────────────────┐     ┌─────────────────────────────┐
│ - Log failure to MySQL (stage=test)    │     │ STAGE 3: CANARY REAL APPLY  │
│ - Attach dry-run output & diffs        │     │  - Apply on same 5 devices  │
│ - Notify change FAILED (no config      │     │  - Compare post-output:     │
│   change executed)                     │     │      desc actually changed  │
│ - End pipeline                         │     │  - Snapshot & validate      │
└────────────────────────────────────────┘     └─────────────┬───────────────┘
                                                            │
                                                ┌───────────┴───────────────┐
                                                │ DECISION D: 5 CANARY OK? │
                                                └───────────┬───────────────┘
                                                            │
               ┌────────────────────────────────────────────┼────────────────────────────────┐
               │                                            │                                │
               ▼                                            ▼                                ▼
       FAIL PATH 2                                   SUCCESS PATH 2                  (optional user input)
       ─────────────────────────────────         ─────────────────────────────────
┌────────────────────────────────────────┐   ┌────────────────────────────────────────┐
│ - Post-check fails on ≥1 canary       │   │ - Mark canary phase SUCCESS            │
│   (e.g. desc not applied, interface   │   │ - Proceed to HUGE rollout              │
│    down, or validation mismatch)      │   │   with batches of 30 devices           │
│ - Execute rollback for those 5        │   └────────────────────────────────────────┘
│ - Log details & rollback to MySQL    │
│ - Notify "Change FAILED at canary"   │
│ - End pipeline                        │
└────────────────────────────────────────┘

(6) MASS ROLLOUT TO REMAINING DEVICES (BATCHES OF 30)
─────────────────────────────────────────────────────────────────────────────
   ┌─────────────────────────────────────────────────────────────┐
   │ STAGE 4: MAIN DEPLOYMENT                                   │
   │  - Determine remaining targets:                            │
   │      * All selected devices minus 5 canaries               │
   │  - Split into batches of 30 (last batch smaller)           │
   │  - For EACH batch (BATCH_N):                               │
   │      1. Render JINJA2 per device                           │
   │      2. Run Ansible APPLY                                  │
   │      3. Record per-device result to MySQL                  │
   │      4. Optional: post-check on each device / sample set   │
   └─────────────┬──────────────────────────────────────────────┘
                 │
                 ▼
   ┌─────────────────────────────────────────────────────────────┐
   │ DECISION E: DID BATCH_N HAVE FAILURES?                      │
   └─────────────┬──────────────────────────────────────────────┘
                 │
      ┌──────────┼─────────────────────────────┐
      │          │                             │
      ▼          ▼                             ▼
 NO FAILURES   SOME FAILURES                   ─ (optional auto policy)
────────────   ────────────────
┌────────────────────────────────────────┐    ┌─────────────────────────────────────┐
│ - Log all devices in batch as success │    │ PROMPT USER:                        │
│ - Proceed to next batch               │    │  "Batch N: X devices failed.        │
└────────────────────────────────────────┘    │   Continue or rollback failed?"     │
                                              └─────────────┬──────────────────────┘
                                                            │
                  ┌─────────────────────────────────────────┼─────────────────────────────┐
                  │                                         │                             │
                  ▼                                         ▼                             ▼
        OPTION E1: USER "CONTINUE"            OPTION E2: USER "ROLLBACK FAILED"   (Option E3: Timeout → policy)
        ───────────────────────────           ─────────────────────────────────    ──────────────────────────────
┌──────────────────────────────────────┐    ┌──────────────────────────────────┐   e.g. default = continue:
│ - Skip failed devices in subsequent │    │ - Run rollback only on failed    │   - mark failed devices in DB
│   batches / retry later             │    │   devices in batch               │   - continue rollout
│ - Mark them as failed in MySQL      │    │ - Log rollback results to MySQL  │
│ - Continue with next batch          │    │ - Decide:                        │
└──────────────────────────────────────┘    │    * stop entire rollout, OR     │
                                            │    * continue with remaining     │
                                            └──────────────────────────────────┘

(7) POST-CHANGE SNAPSHOTS & VALIDATION (ALL TOUCHED DEVICES)
─────────────────────────────────────────────────────────────────────────────
   ┌─────────────────────────────────────────────────────────────┐
   │ STAGE 5: POST-CHANGE SNAPSHOT & TESTS                      │
   │  - Collect new "show run" / "show interface"               │
   │  - Run pyATS/validation:                                  │
   │       * verify new description present                    │
   │       * verify interfaces UP (as required)                │
   │  - Generate diffs: pre vs post                            │
   │  - Update MySQL results per device                        │
   └─────────────┬──────────────────────────────────────────────┘
                 │
                 ▼
(8) DECLARATIVE POST ACTIONS, INVENTORY & VISIBILITY
─────────────────────────────────────────────────────────────────────────────
   ┌─────────────────────────────────────────────────────────────┐
   │ STAGE 6: DECLARATIVE POST ACTIONS                          │
   │  - Cleanup temp secrets / Jinja2 artifacts                 │
   │  - Update INVENTORY:                                       │
   │       * include only devices that actually changed         │
   │       * record offline or skipped devices                  │
   │  - Update MySQL:                                           │
   │       * pipeline_runs (summary per change)                 │
   │       * device_change_logs (per device)                    │
   │  - Send notifications:                                     │
   │       * Email / Slack: success / failure / partial         │
   │       * Include change ID (CHG-1234), stats, links         │
   │  - If failure threshold exceeded → auto "change failed"    │
   │       * optional global rollback                           │
   └─────────────┬──────────────────────────────────────────────┘
                 │
                 ▼
(9) GRAFANA & REPORTING
─────────────────────────────────────────────────────────────────────────────
   ┌─────────────────────────────────────────────────────────────┐
   │   MySQL DATABASE                                            │
   │   - pipeline_runs (per change / run)                        │
   │   - device_change_logs (per router / per batch)             │
   └─────────────┬──────────────────────────────────────────────┘
                 │
         Grafana MySQL Datasource (and optional Prometheus)
                 │
                 ▼
   ┌─────────────────────────────────────────────────────────────┐
   │   GRAFANA DASHBOARDS                                        │
   │   - Timeline: changes over time, x-axis = date/time         │
   │   - CHG-1234 summary: success/failed/partial                │
   │   - Per-device table: 300 routers, status, batch, duration  │
   │   - Filter by: change_id, device group, region              │
   │   - Drill-down links to Jenkins logs & diffs                │
   └─────────────────────────────────────────────────────────────┘

